/**
 * Live chat for the contact page.
 *
 * Paste your tawk.to Property ID and Widget ID below and the contact page grows a
 * "Live chat" card. Until then the card stays hidden and visitors are
 * offered the form, email and phone instead — nothing breaks while this is blank.
 *
 * Find both in tawk.to → Administration → Chat Widget. The embed snippet looks
 * like https://embed.tawk.to/<propertyId>/<widgetId>.
 *
 * Note: the button only appears when an agent is actually online. An unattended
 * chat box is worse than none, so when nobody is available the card falls
 * back to a message-and-email prompt automatically.
 */
window.FELICAN_TAWK = {
  propertyId: '6a7a17446f4cb41d46e4af90',
  widgetId: '1jvmelp8f',
};
