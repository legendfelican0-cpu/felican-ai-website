# Felican AI Website

Three light, Claude-inspired website directions for Felican AI.

## Commands

- `npm run dev` — run the local concept site
- `npm run build` — create a production build
- `npm test` — run component tests
- `npm run test:e2e` — run browser tests
